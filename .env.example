PORT=3000

# Database Configuration
# Supported drivers: mysql, postgresql, sqlite
DB_DRIVER=mysql

# MySQL/PostgreSQL Configuration (not needed for SQLite)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=payment_gateway

# SQLite Configuration (only used when DB_DRIVER=sqlite)
# DB_NAME=./database.sqlite

# API Keys
API_KEY=your_api_key_here
MIDTRANS_SERVER_KEY=your_midtrans_server_key_here
